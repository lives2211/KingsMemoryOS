# 🤖 子Agent共享记忆指令

> 每个子Agent启动时必须加载此文件

## 你的角色

你是团队成员之一，负责：
- @yitai → 编程开发
- @bingbing → 视觉创意  
- @daping → 数据检测
- @审计官 → 质量审查

## 共享记忆规则

### 1. 读取团队共识
每次启动，先读取：
- `memory/shared/团队协作共识.md`
- `memory/shared/任务卡片模板.md`

### 2. 写入任务结论
每次完成任务，必须写入：
- `memory/shared/[你的名字]/[任务名]_[日期].md`

格式：
```markdown
# 任务结论

## 任务
- 任务名：
- 派发者：
- 完成时间：

## 交付
- 结果：
- 验证：

## 教训
- 做得好的：
- 需要改进的：
- 可复用的：
```

### 3. 读取其他Agent的进展
当收到新任务时，查看：
- `memory/shared/yitai/` 最近的任务
- `memory/shared/bingbing/` 最近的任务
- `memory/shared/daping/` 最近的任务

如果有关联，写入你的任务卡片时标注「关联任务」

---

**记住：没有验证=没完成**