# 2026-02-11

## Telegram 路由问题排查

**问题**: Telegram 消息能到达 gateway（有消息ID记录），但未路由到主会话 agent:main:main。

**发现**:
- Telegram 配置存在于 `~/.openclaw/openclaw.json` 的 `channels.telegram` 部分
- 配置包含 `dmPolicy: allowlist`，允许用户 `6615665748`
- 但缺少 `sessionKey` 字段导致消息无法路由

**配置状态**:
```json
"channels": {
  "telegram": {
    "enabled": true,
    "dmPolicy": "allowlist",
    "botToken": "8324074683:AAHsiW9dTrzsp2R0iT-TC1_UPGq5lXv2Y0k",
    "allowFrom": ["6615665748"],
    "groupPolicy": "allowlist",
    "streamMode": "partial",
    "proxy": "http://127.0.0.1:10808"
  }
}
```

**尝试的修复**:
- `openclaw config set channels.telegram.sessionKey "agent:main:main"` → 失败 (Unrecognized key)
- 尝试添加 sessions 配置 → 配置路径不存在

**待解决**: 需要找出正确的配置方式来设置 Telegram 消息路由到主会话。尝试过重启 gateway 但未确认是否解决。

## 其他
- Gateway 自动更新到 2026.2.9（小补丁更新）
